<Window x:Class="krrTools.Tools.KRRLV.KRRLVWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:krrTools.Tools.KRRLV"
        mc:Ignorable="d"
        Title="Krr LV Calculator" Height="540" Width="960"
        AllowDrop="True" Drop="Window_Drop" DragEnter="Window_DragEnter">
    
    <Window.Resources>
        <!-- 浅灰主题按钮样式 -->
        <Style x:Key="LightGrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2196F3"/>
                                <Setter Property="BorderBrush" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 浅灰主题DataGrid样式 -->
        <Style x:Key="LightGrayDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
        </Style>
        
        <!-- 浅灰主题DataGrid单元格样式 -->
        <Style x:Key="LightGrayDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#212121"/>
        </Style>
        
        <!-- 浅灰主题DataGrid列头样式 -->
        <Style x:Key="LightGrayDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
        </Style>
        
        <!-- 窗口全局样式 -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 文件列表显示区域 -->
        <DataGrid Grid.Row="0" 
                  Name="FileDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ItemsSource="{Binding OsuFiles}"
                  Style="{StaticResource LightGrayDataGridStyle}"
                  CellStyle="{StaticResource LightGrayDataGridCellStyle}"
                  ColumnHeaderStyle="{StaticResource LightGrayDataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="KRRLV" Binding="{Binding KRRLV, StringFormat=F2}" Width="60"/>
                <DataGridTextColumn Header="XXYSR" Binding="{Binding XXYSR, StringFormat=F2}" Width="60"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Diff" Binding="{Binding Diff}" Width="*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="*"/>
                <DataGridTextColumn Header="Creator" Binding="{Binding Creator}" Width="*"/>
                <DataGridTextColumn Header="Keys" Binding="{Binding Keys}" Width="60"/>
                <DataGridTextColumn Header="BPM" Binding="{Binding BPM}" Width="80"/>
                <DataGridTextColumn Header="OD" Binding="{Binding OD}" Width="60"/>
                <DataGridTextColumn Header="HP" Binding="{Binding HP}" Width="60"/>
                <DataGridTextColumn Header="LN%" Binding="{Binding LNPercent, StringFormat=F2}" Width="60"/>
                <DataGridTextColumn Header="beatmapID" Binding="{Binding BeatmapID}" Width="80"/>
                <DataGridTextColumn Header="setID" Binding="{Binding BeatmapSetID}" Width="80"/>
                <DataGridTextColumn Header="FilePath" Binding="{Binding FilePath}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- 底部按钮区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 添加的文字说明 -->
            <TextBlock Grid.Column="0" 
                       VerticalAlignment="Center"
                       Foreground="#212121">
                <Run Text="拖拽.osu文件或包含.osu的文件夹到此窗口。" FontSize="18"/>
                <LineBreak/>
                <Run Text="Drag and drop .osu files or a folder containing .osu files to this window." FontSize="14"/>
            </TextBlock>
            <!-- 删除了TextBox和浏览按钮 -->
            <Button Grid.Column="1" 
                    Content="Save" 
                    FontSize="18"
                    Width="120" Height="40"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource LightGrayButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
