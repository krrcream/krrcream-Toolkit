<Window x:Class="krrTools.tools.LNTransformer.LNTransformer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        Title="LNTransformer"
        Width="555" Height="615"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- 浅灰主题按钮样式 -->
        <Style x:Key="LightGrayButtonStyle" TargetType="Button">            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2196F3"/>
                                <Setter Property="BorderBrush" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 浅灰主题滑块样式 -->
        <Style x:Key="LightGraySliderStyle" TargetType="Slider">            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
        </Style>

        <!-- 浅灰主题文本框样式 -->
        <Style x:Key="LightGrayTextBoxStyle" TargetType="TextBox">            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <!-- 浅灰主题复选框样式 -->
        <Style x:Key="LightGrayCheckBoxStyle" TargetType="CheckBox">            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
        </Style>

        <!-- 浅灰主题进度条样式 -->
        <Style x:Key="LightGrayProgressBarStyle" TargetType="ProgressBar">            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Foreground" Value="#2196F3"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="15" HorizontalAlignment="Stretch">
        <!-- Level 控件 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <Slider Style="{StaticResource LightGraySliderStyle}" Maximum="10" Minimum="-3" Value="3" IsSnapToTickEnabled="True" TickFrequency="1" x:Name="LevelValue" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Slider.ToolTip>
                    <TextBlock>
                        <Run Text="Level -3: Hold off"/>
                        <LineBreak/>
                        <Run Text="Level -2: Random ms"/>
                        <LineBreak/>
                        <Run Text="Level -1: Random divide"/>
                        <LineBreak/>
                        <Run Text="Level 0: Regular LN"/>
                        <LineBreak/>
                        <Run Text="Level 3: Light LN"/>
                        <LineBreak/>
                        <Run Text="Level 5: Medium LN"/>
                        <LineBreak/>
                        <Run Text="Level 7: Dense LN"/>
                        <LineBreak/>
                        <Run Text="Level 10: Full LN"/>
                    </TextBlock>
                </Slider.ToolTip>
            </Slider>
            <TextBlock Text="{Binding ElementName=LevelValue, Path=Value, StringFormat='Level {0}'}" HorizontalAlignment="Center" 
                       Margin="0,5,0,0" FontSize="20" FontWeight="Bold">
                <TextBlock.ToolTip>
                    <TextBlock>
                        <Run Text="Level -3: Hold off"/>
                        <LineBreak/>
                        <Run Text="Level -2: Random ms"/>
                        <LineBreak/>
                        <Run Text="Level -1: Random divide"/>
                        <LineBreak/>
                        <Run Text="Level 0: Regular LN"/>
                        <LineBreak/>
                        <Run Text="Level 3: Light LN"/>
                        <LineBreak/>
                        <Run Text="Level 5: Medium LN"/>
                        <LineBreak/>
                        <Run Text="Level 7: Dense LN"/>
                        <LineBreak/>
                        <Run Text="Level 10: Full LN"/>
                    </TextBlock>
                </TextBlock.ToolTip>
            </TextBlock>
        </StackPanel>

        <!-- LN Percentage 控件 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <Slider Style="{StaticResource LightGraySliderStyle}" Maximum="100" Minimum="0" Value="100" IsSnapToTickEnabled="True" TickFrequency="1" x:Name="PercentageValue"/>
            <TextBlock Text="{Binding ElementName=PercentageValue, Path=Value, StringFormat='LN {0}%'}" HorizontalAlignment="Center" 
                       Margin="0,5,0,0" TextWrapping="Wrap" FontSize="20" FontWeight="Bold"/>
        </StackPanel>

        <!-- Divide 控件 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <Slider Style="{StaticResource LightGraySliderStyle}" Maximum="16" Minimum="1" Value="4" IsSnapToTickEnabled="True" TickFrequency="1" x:Name="DivideValue"/>
            <TextBlock Text="{Binding Value, ElementName=DivideValue, StringFormat=Divide 1/\{0\}}" HorizontalAlignment="Center" 
                       Margin="0,5,0,0" TextWrapping="Wrap" FontSize="20" FontWeight="Bold"/>
        </StackPanel>

        <!-- Column 控件 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <Slider Style="{StaticResource LightGraySliderStyle}" Maximum="10" Minimum="0" Value="10" IsSnapToTickEnabled="True" TickFrequency="1" x:Name="ColumnValue"/>
            <TextBlock Text="{Binding Value, ElementName=ColumnValue, StringFormat=Column \{0\}}" HorizontalAlignment="Center" 
                       Margin="0,5,0,0" TextWrapping="Wrap" FontSize="20" FontWeight="Bold"/>
        </StackPanel>

        <!-- Gap 控件 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <Slider Style="{StaticResource LightGraySliderStyle}" Maximum="20" Minimum="0" Value="12" IsSnapToTickEnabled="True" TickFrequency="1" x:Name="GapValue"/>
            <TextBlock Text="{Binding Value, ElementName=GapValue, StringFormat=Gap \{0\}}" HorizontalAlignment="Center" 
                       Margin="0,5,0,0" TextWrapping="Wrap" FontSize="20" FontWeight="Bold"/>
        </StackPanel>

        <!-- Overall Difficulty 控件 -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Overall Difficulty" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="OverallDifficulty" Text="0" Style="{StaticResource LightGrayTextBoxStyle}" Width="150" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- CheckBoxes 控件 -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <CheckBox x:Name="Ignore" Content="Ignore" Style="{StaticResource LightGrayCheckBoxStyle}" IsChecked="True" FontSize="18" Margin="0,0,20,0"/>
            <CheckBox x:Name="FixError" Content="Fix Error" Style="{StaticResource LightGrayCheckBoxStyle}" IsChecked="True" FontSize="18" Margin="0,0,20,0"/>
            <CheckBox x:Name="OriginalLN" Content="Original LN" Style="{StaticResource LightGrayCheckBoxStyle}" IsChecked="False" FontSize="18"/>
        </StackPanel>

        <!-- Drop Zone -->
        <TextBlock Text="Drop Folder or .osu File" VerticalAlignment="Center" 
                   HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Margin="0,10,0,10" />
        
        <!-- Instruction 超链接 -->
        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10">
            <Hyperlink Click="InstructionButton_Click">
                <Run Text="Instruction" Foreground="Blue" TextDecorations="Underline"/>
            </Hyperlink>
        </TextBlock>
        
        <!-- OSU Listener relocated to main tab bar -->
        
        <!-- Progress StackPanel -->
        <StackPanel x:Name="ProgressStackPanel" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,60">
            <ProgressBar x:Name="ConversionProgress" Minimum="0" Maximum="100" Height="20" Width="400" Margin="0,0,0,5" Style="{StaticResource LightGrayProgressBarStyle}"/>
            <TextBlock x:Name="ProgressText" Text="0/0" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold"/>
        </StackPanel>

        
    </StackPanel>
</Window>