<Window x:Class="krrTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:krrTools"
        mc:Ignorable="d"
        Title="krrcream's Toolkit V2.2.0" Height="400" Width="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- 浅灰主题按钮样式 -->
        <Style x:Key="LightGrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2196F3"/>
                                <Setter Property="BorderBrush" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 窗口全局样式 -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Converter Button -->
            <Button Content="Converter" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="200" Height="50"
                    Click="ConverterButton_Click"
                    Margin="0,10,0,15"
                    Style="{DynamicResource LightGrayButtonStyle}"/>
            
            <!-- LN Transformer Button -->
            <Button Content="LN Transformer" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="200" Height="50"
                    Click="LNTransformerButton_Click"
                    Margin="0,0,0,15"        Style="{DynamicResource LightGrayButtonStyle}"/> 

            <!-- DP TOOL Button -->
            <Button Content="DP tool" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="200" Height="50"
                    Click="DPToolButton_Click"
                    Margin="0,0,0,15"
                    Style="{DynamicResource LightGrayButtonStyle}"/>
            
            <Button Content="LV Calculator" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="200" Height="50"
                    Click="KRR_LV_Button_Click"
                    Margin="0,0,0,15"
                    Style="{DynamicResource LightGrayButtonStyle}"/>
            
            <!-- osu! file manager Button -->
            <Button Content="osu! file manager" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="200" Height="50"
                    Click="GetFilesButton_Click"
                    Margin="0,0,0,10"
                    Style="{DynamicResource LightGrayButtonStyle}"/>
            
            <!-- GitHub 链接 -->
            <TextBlock HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,5">
                <Run Text="© 2025 krrcream. All rights reserved."/>
                <Hyperlink NavigateUri="https://github.com/krrcream/krrcream-Toolkit" RequestNavigate="Hyperlink_RequestNavigate">
                    github
                </Hyperlink>
            </TextBlock>
            
        </StackPanel>
    </Grid>
</Window>
