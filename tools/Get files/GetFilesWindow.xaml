<Window x:Class="krrTools.tools.Get_files.GetFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:getFiles="clr-namespace:krrTools.tools.Get_files"
        mc:Ignorable="d"
        Title="osu! file manager V0.1" Height="540" Width="960"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Style="{DynamicResource ToolWindowStyle}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <getFiles:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        
        <!-- 浅灰主题按钮样式 -->
        <Style x:Key="LightGrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2196F3"/>
                                <Setter Property="BorderBrush" Value="#1976D2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 浅灰主题文本框样式 -->
        <Style x:Key="LightGrayTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
        
        <!-- 浅灰主题DataGrid样式 -->
        <Style x:Key="LightGrayDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
        </Style>
        
        <!-- 浅灰主题DataGrid单元格样式 -->
        <Style x:Key="LightGrayDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#212121"/>
        </Style>
        
        <!-- 浅灰主题DataGrid行样式 -->
        <Style x:Key="LightGrayDataGridRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 浅灰主题DataGrid列头样式 -->
        <Style x:Key="LightGrayDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
        </Style>
        
        <!-- 浅灰主题进度条样式 -->
        <Style x:Key="LightGrayProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Foreground" Value="#2196F3"/>
        </Style>
        
        <!-- 窗口全局样式 (keyed to avoid implicit application when resources are merged) -->
        <Style x:Key="ToolWindowStyle" TargetType="Window">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 搜索行 -->
            <RowDefinition Height="*"/>     <!-- DataGrid -->
            <RowDefinition Height="Auto"/>  <!-- 底部按钮区域 -->
        </Grid.RowDefinitions>

        <!-- 搜索行 -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding TitleFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="1" Text="{Binding DiffFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="2" Text="{Binding ArtistFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="3" Text="{Binding CreatorFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="4" Text="{Binding KeysFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="5" Text="{Binding OdFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="6" Text="{Binding HpFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="7" Text="{Binding BeatmapIdFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="8" Text="{Binding BeatmapSetIdFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
            <TextBox Grid.Column="9" Text="{Binding FilePathFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Width="Auto" Height="20" Margin="0,0,0,5" 
                     Style="{StaticResource LightGrayTextBoxStyle}"/>
        </Grid>

        <!-- 文件列表显示区域 -->
        <DataGrid Grid.Row="1" 
                  Name="FileDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ItemsSource="{Binding FilteredOsuFiles}"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  Style="{StaticResource LightGrayDataGridStyle}"
                  CellStyle="{StaticResource LightGrayDataGridCellStyle}"
                  RowStyle="{StaticResource LightGrayDataGridRowStyle}"
                  ColumnHeaderStyle="{StaticResource LightGrayDataGridColumnHeaderStyle}">
            <!-- 添加右键菜单 -->
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Delete" Click="DeleteMenuItem_Click" 
                              Foreground="Black"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Diff" Binding="{Binding Diff}" Width="*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="*"/>
                <DataGridTextColumn Header="Creator" Binding="{Binding Creator}" Width="*"/>
                <DataGridTextColumn Header="Keys" Binding="{Binding Keys}" Width="60"/>
                <DataGridTextColumn Header="OD" Binding="{Binding OD}" Width="60"/>
                <DataGridTextColumn Header="HP" Binding="{Binding HP}" Width="60"/>
                <DataGridTextColumn Header="beatmapID" Binding="{Binding BeatmapID}" Width="80"/>
                <DataGridTextColumn Header="setID" Binding="{Binding BeatmapSetID}" Width="80"/>
                <DataGridTextColumn Header="FilePath" Binding="{Binding FilePath}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 底部按钮区域 -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 进度条 -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center"
                        Margin="0,0,10,0">
                <ProgressBar Width="200" 
                             Height="20"
                             Value="{Binding ProgressValue}"
                             Maximum="{Binding ProgressMaximum}"
                             Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Style="{StaticResource LightGrayProgressBarStyle}"/>
                <TextBlock Text="{Binding ProgressText}" 
                           Foreground="#212121"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <!-- 添加的文件夹选择按钮 -->
            <Button Grid.Column="1" 
                    Content="Set Songs Path" 
                    FontSize="18"
                    Width="160" Height="40"
                    Command="{Binding SetSongsFolderCommand}"
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}"
                    Style="{StaticResource LightGrayButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
